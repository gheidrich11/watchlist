# Get a free API key at https://www.themoviedb.org/settings/api
# Use the v3 auth "API Key" (not the v4 read access token)
TMDB_API_KEY=your_key_here

# SQLite file location. The path is relative to the prisma/ directory.
DATABASE_URL="file:./dev.db"
